import React from 'react';

import { SelectedCount } from './SelectedCount';
import { UserList } from './UserList';

export default () => {
    return (
        <div>
            <SelectedCount />
            <UserList />
        </div>
    );
};
